const Schema = require('../model/index.schema');
const moment = require('moment');
module.exports = {
    stats: async function (req, res){

    },
    teacher_stats: async function (req, res){
        Promise.all().then().catch
    }
};